---
title: "论文完善"
date: 2026-03-15 13:28:22
categories:
- 课题项目
- 法底捞针_基于AI智能主持人的海龟汤式互动反诈网站设计研究
---

1. 可控 AI 判题机制：不是直接问大模型，而是“规则约束 + 结果校验 + 预制答案 + 缓存一致性 + 重试/降级”。  
  证据：aiService.js:70 aiService.js:253 aiService.js:288 aiService.js:384
2. 提示生成个性化：后端把玩家历史提问拼进 prompt，提示“接近答案但不泄底”。  
证据：ai.js:184 ai.js:187
3. 双线叙事解锁链路：完成现代+古代线后，写入 dualStoryCompleted，解锁 core_insight。  
证据：DualStoryPage.tsx:172 DualStoryPage.tsx:207
4. 工程韧性设计：Mongo 不可用时自动降级内存模型，服务仍可运行。  
证据：database.js:19 models/index.js:5 initData.js:14

// 可控判题：预制答案 + 一致性缓存  
  if (this.answerCache.has(cacheKey)) return this.answerCache.get(cacheKey);  
  const predefinedAnswer = this.checkPredefinedAnswer(question, storyContext, storyId);  
  if (predefinedAnswer) {  
    this.answerCache.set(cacheKey, predefinedAnswer);  
    return predefinedAnswer;  
  }

  来源：aiService.js:294

  // 双线完成后解锁核心启示关卡  
  localStorage.setItem('dualStoryProgress', JSON.stringify({  
    ...progress,  
    completedStories,  
    dualStoryCompleted: completedStories.length >= 2  
  }));  
  if (!gameProgress.unlockedStories.includes('core_insight')) {  
    gameProgress.unlockedStories.push('core_insight');  
  }

  来源：DualStoryPage.tsx:172

  // 前端把玩家提问历史传给后端，生成个性化提示  
  const response = await fetch(getApiUrl('/api/ai/chat'), {  
    method: 'POST',  
    body: JSON.stringify({  
      context: 'turtle_soup_hint',  
      storyId: selectedStory.id,  
      playerQuestions  
    })  
  });

  来源：GamePage.tsx:459

  // 数据库不可用时降级运行  
  const allowInMemoryFallback = process.env.ALLOW_INMEMORY_DB_FALLBACK === 'true';  
  if (process.env.NODE_ENV === 'development' || allowInMemoryFallback) {  
    return false;  
  }

  来源：database.js:19



1. room.js 控制器目前不存在，你的后端实际控制器是 auth.js / game-simple.js / ai.js / dualStory.js / mentorStory.js。  
  文件证据：controllers 目录
2. Redis：配置已实现 这句不准确。项目里只有 redis 依赖，源码未看到 createClient/连接逻辑。  
文件证据：backend/package.json:1
3. WebSocket 已实现 也应改成“前端接口占位，主流程未接入”。socket.ts 是 no-op stub。  
文件证据：socket.ts:1
4. game-simple.js 是内存示例题，不是完整题库服务。  
文件证据：game-simple.js:4
5. 进度存储应写为“localStorage 为主，sessionStorage 为辅”，不是纯 sessionStorage。  
文件证据：GamePage.tsx:376 GamePlayPage.tsx:16
6. Zustand 属于“已安装未使用”，建议在论文写清。  
文件证据：frontend/package.json:1

