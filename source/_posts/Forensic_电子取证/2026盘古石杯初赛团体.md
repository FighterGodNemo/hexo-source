---
title: 2026盘古石杯初赛团体
date: 2026-05-10 15:10:00
updated: 2026-05-10T17:09
categories: Forensic_电子取证
tags:
  - 电子取证
  - Forensic
  - 2026盘古石杯初赛团体
created: 2026-05-10T17:07
---

## 答案速览

| 题号/问题 | 答案 | 状态 |
| --- | --- | --- |
| 黄志远手机安装的短视频应用数量 | `4` | 已确认 |
| `gptos.intelligence.assistant` 首次打开时间 | `2026-04-17-14:39:39` | 已确认 |
| 黄志远使用 `gptos.intelligence.assistant` 攻击过多少台主机 | `1` | 当前证据支持，建议后续继续从 OpenClaw 本地日志复核 |
| 黄志远控制 PC agent 的应用 | `discord` | 已确认 |
| 该控制应用版本 | `311.20` | 已确认 |
| 该控制应用登录用户名 | `test901234.` | 已确认 |
| 该应用与 PC agent 的配对码 | `3EXEQ5R8` | 已确认 |
| 该应用共扫描 IP 数量 | `3` | 已确认 |
| 该应用调用的暴力破解工具数量 | `1` | 已确认 |
| 黄志远内部通联工具账号登录密码 | 待复核 | 已提取 `user.password_hash/password_salt`，不把 `flutter.db_password` 混作登录密码 |
| 黄志远发给代号“军师”的文件数量 | `2` | 已确认 |
| 方俊朗筛选优质客户应用包名 | `com.example.predictor` | 已确认 |
| 方俊朗优质客户应用加密数据库 | `chat_history.db` | 已确认 |
| 方俊朗优质客户应用数据库密钥派生算法/迭代次数 | `PBKDF2WithHmacSHA256` / `10000` | 已确认 |
| 方俊朗内部通联工具加入群数量 | `2` | 已确认 |
| 方俊朗通过物联网设备漏洞获得的用户信息数量 | `32` | 已确认 |
| 周文杰内部通联工具包名 | `com.socialchat.social_chat_app` | 已确认 |
| 周文杰内部通联 app 聊天数据库名称 | `social_chat.db` | 已确认 |
| 周文杰内部通联 app 聊天数据库密码保存文件 | `FlutterSharedPreferences.xml` | 已确认 |
| 周文杰内部通联 app 聊天数据库密码 | `Pgs-dbw1776853545473Good` | 已确认 |
| 内部通联 app 聊天数据加密算法 | `AES` | 已确认 |
| 周文杰内部通联 app 用户密码盐值 | `a3f8d9c2e1b4h7g6k9m2n5p8q1r4t7w` | 已确认 |
| 记录周文杰登录密码提示的应用包名 | `com.jinghong.notebookkssjh` | 已确认 |
| 周文杰内部通联 app 登录密码 | `mb202623` | 已确认 |
| 周文杰删除的聊天记录数量 | `5` | 已确认 |
| 聊天数据库中显示删除状态的字段 | `state_bits` | 已确认 |
| 黄志远 car 碰撞瞬间大灯模式 | `HIGH_BEAM` | 已确认 |
| 黄志远 car 被覆写远程代理 IP | `45.33.22.11` | 已确认 |
| 黄志远 car ELF 后门绝对路径 | `/data/local/tmp/syslogd_update` | 已确认 |
| 黄志远 car 采样点 0 纵向车速 | `180` | 已确认 |
| 黄志远 car 碰撞前 5 秒纵向车速 | `85` | 已确认 |
| 黄志远 car 伪装系统日志服务进程名 | `deadlive_solologd` | 高置信度碎片重组 |
| 林小婉隐藏信息应用程序名称 | `今日头条` | 已确认 |
| 林小婉隐藏应用 PIN | `20150412` | 已确认 |
| 林小婉隐藏数据 notes 字段含义 | `备注` | 已确认 |
| 林小婉文件备份服务器 IP | `192.168.1.200` | 已确认 |
| 财神撤回消息内容 | `把23年10月到24年6月的账本整理一下，发给我。` | 已确认 |
| 林小婉账本打开密码 | `DragonTeng@2024#$` | 已确认 |
| 林小婉手表型号 | `HUAWEI WATCH GT 5` | 已确认 |
| 林小婉实际年龄 | `37` | 交叉推断，待手表块直解复核 |

## 一、手机取证

### 1. 黄志远 phone.E01

检材类型：Android 手机镜像  
原始检材：`K:\黄志远\phone.E01`  
本地工作目录：`C:\Users\glj07\Desktop\Codex工作区\Writeup\Forensic\2026盘古石杯初赛团体\黄志远phone`  
SHA256：`DC9F7629643E8BE02BB73A2FD084A22193EBCA915BB50A57A62FC3D24064FF1F`

#### 1.1 短视频应用安装数量

题目：黄志远手机总共安装了多少款短视频应用？

证据类型：Android 包管理记录  
来源路径：`exports/android/packages.xml`、`exports/android/packages.list`  
关键文件：`packages.xml`、`packages.list`  
方法：检索已安装包名，并按题目口径排除 `Bilibili` 等视频平台类应用。

```powershell
Select-String -LiteralPath exports/android/packages.xml -Pattern '<package name="com\.ss\.android\.ugc\.aweme\.lite"|<package name="com\.ss\.android\.ugc\.aweme"|<package name="com\.kuaishou\.nebula"|<package name="com\.smile\.gifmaker"'
```

关键摘录：

```text
com.ss.android.ugc.aweme.lite
com.ss.android.ugc.aweme
com.kuaishou.nebula
com.smile.gifmaker
```

推理：上述 4 个包名分别对应抖音极速版、抖音、快手极速版、快手。`tv.danmaku.bili` 与 `com.sobey.fc.component.btvvideo` 属于视频平台/视频应用，不按本题“短视频应用”口径计入。

结论：

```text
4
```

#### 1.2 `gptos.intelligence.assistant` 首次打开时间

题目：`gptos.intelligence.assistant` 应用首次打开应用的时间是？

证据类型：Android UsageStats 前台事件  
来源路径：`exports/android/usagestats/daily/1776388876473`  
关键文件：`daily/1776388876473`  
方法：从 `/data/system/usagestats/0` 提取 daily 文件，查找该包名首次 `type=1` 前台事件，并用文件基准时间加事件相对毫秒数换算为北京时间。

关键摘录：

```xml
<event time="19103003" package="gptos.intelligence.assistant" class="app.anyclaw.MainActivity" flags="0" type="1" />
```

时间换算：

```text
1776388876473 + 19103003 = 1776407979476
Asia/Shanghai = 2026-04-17 14:39:39
```

结论：

```text
2026-04-17-14:39:39
```

#### 1.3 `gptos.intelligence.assistant` 攻击主机数量

题目：黄志远使用 `gptos.intelligence.assistant` 攻击过多少台主机？

证据类型：手机应用数据与控制消息交叉分析  
来源路径：`exports/android/appdata/gptos.intelligence.assistant/files/rootfs/usr/local/bin/openclaw`、`exports/openclaw-gptos/package.json`、`exports/android/appdata/com.discord/files/kv-storage/@account.1457974771206852664/a`  
关键文件：OpenClaw 启动脚本、OpenClaw `package.json`、Discord `messages0`  
方法：确认 `gptos.intelligence.assistant` 内置 OpenClaw 环境后，结合手机 Discord 中的控制消息与应用安装/首次打开时间过滤攻击目标。

关键摘录：

```text
rowid 39: 对 192.168.61.135 进行渗透提权
rowid 106: 对 192.168.61.135 执行 ping、nmap、gobuster、sqlmap、hydra 等命令
```

推理：`192.168.1.10`、`192.168.1.16` 的扫描记录发生在 `2026-04-11`，早于 `gptos.intelligence.assistant` 安装和首次打开，不计入“使用此应用”。当前与该应用使用时段闭合的攻击目标为 `192.168.61.135`。

证据状态：当前答案为 `1`；该题后续最好继续从 OpenClaw 本地日志中复核，避免只依赖 Discord 控制消息链。

结论：

```text
1
```

#### 1.4 PC agent 控制应用、版本、用户名与配对码

题目：黄志远使用哪款应用控制了其 PC 的 agent 工具？该应用版本、登录用户名、配对码分别是什么？

证据类型：手机 Discord 应用数据  
来源路径：`exports/android/appdata/com.discord/files/kv-storage/@account.1457974771206852664/a`、`exports/android/appdata/com.discord/shared_prefs/CacheStore.xml`、`exports/android/appdata/com.discord/shared_prefs/DiscordNotificationClient.xml`、`exports/android/packages.xml`  
关键文件：Discord `messages0`、`CacheStore.xml`、`DiscordNotificationClient.xml`、`packages.xml`  
方法：解析 Discord kv-storage 的 `messages0.data` JSON blob，读取 pairing 消息；再从 Discord 偏好设置和安装记录读取版本与登录用户。

关键摘录：

```text
messages0 rowid 15:
Here's your pairing code: 3EXEQ5R8
hermes pairing approve discord 3EXEQ5R8
```

```text
CacheStore.xml: client_version 311.20 - rn
DiscordNotificationClient.xml: username test901234.
packages.xml: version=311020
```

推理：题目问“手机上使用哪款应用控制 PC agent”，PC agent 是被控制对象；手机证据中 pairing 命令明确写出 `discord`。

结论：

| 问题 | 答案 |
| --- | --- |
| 控制应用 | `discord` |
| 应用版本 | `311.20` |
| 登录用户名 | `test901234.` |
| 配对码 | `3EXEQ5R8` |

#### 1.5 扫描 IP 数量与暴力破解工具数量

题目：该应用共对几个 IP 进行扫描？该应用总共调用了几个暴力破解工具？

证据类型：手机 Discord 控制消息  
来源路径：`exports/android/appdata/com.discord/files/kv-storage/@account.1457974771206852664/a`  
关键文件：Discord `messages0`  
方法：解析 `messages0.data` JSON blob，对扫描/任务消息中的 IPv4 地址去重，并统计暴力破解工具名。

关键摘录：

```text
扫描 IP 去重：192.168.1.10、192.168.1.16、192.168.61.135
messages0 rowid 106: hydra
```

推理：扫描目标按 IP 去重共 3 个；暴力破解相关命令中只出现 `hydra` 这一类工具。

结论：

| 问题 | 答案 |
| --- | --- |
| 扫描 IP 数量 | `3` |
| 暴力破解工具数量 | `1` |

#### 1.6 内部通联工具登录密码复核状态

题目：黄志远使用其内部通联工具进行沟通，其账号的登陆密码是多少？

证据类型：内部通联应用数据库与偏好设置  
来源路径：`exports/android/appdata/com.socialchat.social_chat_app/shared_prefs/FlutterSharedPreferences.xml`、`exports/android/appdata/com.socialchat.social_chat_app/databases/social_chat.db`  
关键文件：`FlutterSharedPreferences.xml`、`social_chat.db`  
方法：先读取偏好设置中的数据库口令，再用真实 SQLCipher key 打开 `social_chat.db` 读取 `user.password_hash/password_salt`。注意：`flutter.db_password` 是数据库口令，不应直接写成账号登录密码。

关键摘录：

```xml
<string name="flutter.db_password">Pgs-dbw1776839203359Good</string>
```

```sql
PRAGMA key='s-dbw1776839203359Goo';
PRAGMA cipher_compatibility=4;
SELECT id,username,nickname,password_hash,password_salt FROM user;
```

```text
usr_heiked|huangzhiyuan|黄志远|fc29eb768c139c05c0bfcb697d9b26d194878a66451b3ab91b202e9710874a63|a3f8d9c2e1b4h7g6k9m2n5p8q1r4t7w
```

结论：账号登录密码仍需按应用口令算法对 `password_hash/password_salt` 继续破解复核；当前不把 `Pgs-dbw1776839203359Good` 当作最终答案。

#### 1.7 发给代号“军师”的文件数量

题目：黄志远一共发送过几个文件给代号军师的嫌疑人？

证据类型：内部通联应用 SQLCipher 数据库  
来源路径：`exports/android/appdata/com.socialchat.social_chat_app/databases/social_chat.db`、`exports/blutter_socialchat/asm/social_chat_app/main.dart`、`exports/blutter_socialchat/asm/social_chat_app/data/database/database_helper.dart`  
关键文件：`social_chat.db`、Blutter 反编译输出  
方法：反编译确认数据库口令由 `flutter.db_password` 截取得到，使用 `s-dbw1776839203359Goo` 打开 SQLCipher 数据库，联查 `message_file`、`message`、`conversation`。

```sql
SELECT mf.id,mf.message_id,m.sender_id,m.conversation_id,c.name,mf.file_name,mf.file_path
FROM message_file mf
JOIN message m ON mf.message_id=m.id
JOIN conversation c ON c.id=m.conversation_id
WHERE m.sender_id='usr_heiked' AND c.name='军师';
```

关键摘录：

```text
att_msg_000167,msg_000167,usr_heiked,conv_d_usr_heiked_usr_junshi_1776759221637,军师,木马.zip,storage/emulated/0/Download/木马.zip
att_msg_000181,msg_000181,usr_heiked,conv_d_usr_heiked_usr_junshi_1776759221637,军师,木马_v1.2.zip,storage/emulated/0/Download/木马_v1.2.zip
```

推理：会话名为 `军师`，发送者 `usr_heiked` 对应黄志远，共两条文件附件记录。

结论：

```text
2
```

### 2. 方俊朗 phone.E01

检材类型：Android 手机镜像  
原始检材：`K:\方俊郎\phone.E01`  
本地工作目录：`C:\Users\glj07\Desktop\Codex工作区\Writeup\Forensic\2026盘古石杯初赛团体\方俊郎phone`

#### 2.1 筛选优质客户的应用包名

题目：方俊朗使用筛选优质客户的应用包名是什么？

证据类型：Android 包管理记录  
来源路径：`exports/android/packages.xml`、`exports/android/packages.list`  
关键文件：`packages.xml`、`packages.list`  
方法：检索 `com.example.predictor` 安装记录，结合 APK 逆向中应用标签 `优质客户AI判定`、`优质客户预测` 确认为题目应用。

```powershell
Select-String -LiteralPath exports/android/packages.xml,exports/android/packages.list -Pattern 'com\.example\.predictor'
```

关键摘录：

```text
packages.xml 第 1318 行：<package name="com.example.predictor" ...>
packages.list 第 32 行：com.example.predictor 10087 1 /data/user/0/com.example.predictor
```

结论：

```text
com.example.predictor
```

#### 2.2 内部通联工具加入群数量

题目：方俊朗使用其内部通联工具时，共加入过几个群？

证据类型：内部通联应用 SQLCipher 数据库  
来源路径：`exports/socialchat/social_chat.db`、`exports/socialchat/FlutterSharedPreferences.xml`  
关键文件：`social_chat.db`  
方法：根据 `FlutterSharedPreferences.xml` 中的 `flutter.user_id=usr_caishangwang` 定位用户，使用真实数据库 key `s-dbw1776826167125Goo` 解密，联查 `conversation_member` 与 `conversation`。

```sql
SELECT COUNT(*) AS groups_joined
FROM conversation_member cm
JOIN conversation c ON cm.conversation_id=c.id
WHERE cm.user_id='usr_caishangwang' AND c.type='G';
```

关键摘录：

```text
2
conv_g_技术部群_usr_caishangwang|G|技术部群
conv_g_话务组群_usr_caishangwang|G|话务组群
```

结论：

```text
2
```

#### 2.3 物联网设备漏洞获取用户信息数量

题目：方俊朗通过物联网设备漏洞，共获得多少用户信息？

证据类型：内部通联聊天明文  
来源路径：`exports/socialchat/messages_decrypted.csv`、`exports/socialchat/social_chat.db`  
关键文件：`messages_decrypted.csv`  
方法：使用 `user.config_data` 中的 `enc_key`、`enc_iv` 对消息内容进行 AES-CBC/PKCS7 解密后，检索漏洞、IoT、高端用户等关键词。

关键摘录：

```text
msg_000055 | 宰相 | usr_caishangwang | 1708826400000 | 宰相，智能家居漏洞利用已经成熟了。
msg_000057 | 宰相 | usr_caishangwang | 1708826640000 | 已经拿到32个高端用户的数据，主要是扫地机器人和安防摄像头。
```

推理：`msg_000057` 明确给出 `32个高端用户的数据`。

结论：

```text
32
```

#### 2.4 优质客户应用 APK 逆向补充

题目：方俊朗使用的“优质客户”应用中，历史记录数据库、密钥派生算法和迭代次数是什么？

证据类型：APK 脱壳后 smali、SharedPreferences、SQLCipher 数据库  
来源路径：`C:\Users\glj07\Desktop\Codex工作区\Writeup\Forensic\2026盘古石杯初赛团体\APK取证`  
关键文件：`exports/com.example.predictor/unpacked_payload/smali_unpacked/classes3/com/example/predictor/ChatDatabaseHelper.smali`、`KeyDeriver.smali`、`app_config.xml`  
方法：静态分析 `ShellApplication` 解密 `assets/payload.dat`，得到业务 `classes3.dex`，再反汇编定位数据库名和密钥派生逻辑。

关键摘录：

```text
DATABASE_NAME = "chat_history.db"
SecretKeyFactory.getInstance("PBKDF2WithHmacSHA256")
PBKDF2_ITERATIONS = 0x2710
key_iterations value="10000"
```

补充校验：APK v2 签名证书 SHA-256、`Pr3d1ct0r`、`v2.0_release_S@lt`、`X9kZ!qW3` 参与派生，PBKDF2-HMAC-SHA256 输出：

```text
93b16df295656676dd760e6dcfd71623e446b51c91b028ac84c88958fc3ad98b
```

其 MD5 与 `app_config.xml` 中的 `db_integrity_check` 对应，可打开 `chat_history.db` 并读取 `chat_records` 表。

结论：

| 问题 | 答案 |
| --- | --- |
| 加密历史记录数据库 | `chat_history.db` |
| 密钥派生算法 | `PBKDF2WithHmacSHA256` |
| 迭代次数 | `10000` |

### 3. 周文杰 Image.zip

检材类型：Android 镜像/导出数据压缩包  
原始检材：`K:\周文杰\Image.zip`  
本地工作目录：`C:\Users\glj07\Desktop\Codex工作区\Writeup\Forensic\2026盘古石杯初赛团体\周文杰Image`  
SHA256：`A81BE1BE3D2FD7F40EEA6048876C8833F842319AF0A59390C88E64018A9D1CBB`

#### 3.1 内部通联工具包名

题目：周文杰跟其犯罪团伙人员内部通联工具包名是？

证据类型：Android 应用私有目录与 APK 安装目录  
来源路径：`exports\data\data\com.socialchat.social_chat_app\`、`exports\data\app\com.socialchat.social_chat_app-0mI0c1YbKCxv1TVxSgK2ZA==\base.apk`  
关键文件：应用数据目录、`base.apk`  
方法：枚举 `exports` 下应用数据目录与安装目录。

关键摘录：

```text
exports\data\data\com.socialchat.social_chat_app\
exports\data\app\com.socialchat.social_chat_app-0mI0c1YbKCxv1TVxSgK2ZA==\base.apk
```

结论：

```text
com.socialchat.social_chat_app
```

#### 3.2 聊天数据库名称、密码保存文件与数据库密码

题目：内部通联 app 聊天数据库名称、密码保存文件、数据库密码分别是什么？

证据类型：应用私有目录与 SharedPreferences  
来源路径：`exports\data\data\com.socialchat.social_chat_app\databases\social_chat.db`、`exports\data\data\com.socialchat.social_chat_app\shared_prefs\FlutterSharedPreferences.xml`  
关键文件：`social_chat.db`、`FlutterSharedPreferences.xml`  
方法：枚举 `databases` 目录并读取 Flutter SharedPreferences 中的 `flutter.db_password`。

关键摘录：

```text
exports\data\data\com.socialchat.social_chat_app\databases\social_chat.db
```

```xml
<string name="flutter.db_password">Pgs-dbw1776853545473Good</string>
```

结论：

| 问题 | 答案 |
| --- | --- |
| 聊天数据库名称 | `social_chat.db` |
| 数据库密码保存文件 | `FlutterSharedPreferences.xml` |
| 聊天数据库密码 | `Pgs-dbw1776853545473Good` |

#### 3.3 聊天数据加密算法

题目：内部通联 app 聊天数据使用的什么加密算法？

证据类型：APK 静态分析  
来源路径：`exports\data\app\com.socialchat.social_chat_app-0mI0c1YbKCxv1TVxSgK2ZA==\base.apk`  
关键文件：`lib/x86_64/libapp.so`、APK 内 `libsqlcipher.so`  
方法：对 Flutter AOT 库和 APK native library 字符串检索 AES、PBKDF、SQLCipher 关键词。

关键摘录：

```text
impl.block_cipher.aes
AESMode
PBKDF2KeyDerivator
libsqlcipher.so
```

推理：应用使用 SQLCipher 存储数据库，消息内容层面还出现 AES 相关实现；题目问聊天数据加密算法，按应用加密组件填写 AES。

结论：

```text
AES
```

#### 3.4 用户密码盐值与登录密码

题目：内部通联 app 用户密码的盐值是？内部通联 app 登录密码是？

证据类型：SQLCipher 数据库解密、哈希复现  
来源路径：`exports\data\data\com.socialchat.social_chat_app\databases\social_chat.db`、`exports\data\data\com.socialchat.social_chat_app\shared_prefs\FlutterSharedPreferences.xml`  
关键文件：`social_chat.db`、`FlutterSharedPreferences.xml`  
方法：通过 Frida Hook `libsqlcipher.so!sqlite3_key` 获取真实数据库 key `s-dbw1776853545473Goo`，读取 `user.password_hash/password_salt`；复现 App 口令算法 `sha256(sha256(password + salt).hexdigest())` 后使用 hashcat 掩码 `?l?l?d?d?d?d?d?d` 破解，并用模拟器登录验证。

关键摘录：

```text
sqlite3_key key.utf8=s-dbw1776853545473Goo
username=zhouwenjie
password_hash=5d85b77d7d6d1a76cd589c3ba21d1839b1dd28568e39f1d2facc3a1b7d2e8bcb
password_salt=a3f8d9c2e1b4h7g6k9m2n5p8q1r4t7w
hashcat result: mb202623
```

实机复核：使用 `zhouwenjie / mb202623` 登录后进入消息页，证明该值是应用登录密码而不是数据库口令。

![](2026盘古石杯初赛团体/zhou_login_success_real.png)

结论：

| 问题     | 答案                                |
| ------ | --------------------------------- |
| 用户密码盐值 | `a3f8d9c2e1b4h7g6k9m2n5p8q1r4t7w` |
| 登录密码   | `mb202623`                        |

#### 3.5 记录登录密码提示的应用包名

题目：记录周文杰内部通联 app 登录密码提示的应用包名是？

证据类型：记事应用数据与数据库  
来源路径：`exports\data\data\com.jinghong.notebookkssjh\databases\NotePal.db`、`exports\data\media\0\Android\data\com.jinghong.notebookkssjh\files\20260423_100403_179.md`  
关键文件：`NotePal.db`、`20260423_100403_179.md`  
方法：查询 `NotePal.db` 的 `gt_note` 表并读取关联附件。

关键摘录：

```text
应用包名：com.jinghong.notebookkssjh
title = mima
preview_content = 英文*2 +数字*6
附件内容：英文*2 +数字*6
```

结论：

```text
com.jinghong.notebookkssjh
```

#### 3.6 删除聊天记录数量与删除字段

题目：周文杰在内部通联 app 中删除了几条聊天记录？聊天数据库中显示聊天数据删除的是哪个字段？

证据类型：SQLCipher 数据库查询与 APK 静态分析  
来源路径：`exports\data\data\com.socialchat.social_chat_app\databases\social_chat.db`、`exports\data\app\com.socialchat.social_chat_app-0mI0c1YbKCxv1TVxSgK2ZA==\lib\x86_64\libapp.so`  
关键文件：`social_chat.db`、`libapp.so`  
方法：静态分析确认消息表存在 `state_bits` 字段和 `_buildDeletedMessage` 删除消息展示逻辑；使用真实数据库 key 查询 `message` 表中删除状态位数量。

关键摘录：

```sql
CREATE TABLE IF NOT EXISTS message (
  id TEXT PRIMARY KEY,
  conversation_id TEXT NOT NULL,
  sender_id TEXT NOT NULL,
  content TEXT NOT NULL,
  type TEXT DEFAULT 'normal',
  status TEXT DEFAULT 'sending',
  reply_id TEXT,
  attachment_id TEXT,
  create_at INTEGER NOT NULL,
  update_at INTEGER NOT NULL,
  extra_val INTEGER DEFAULT 0,
  flag_data INTEGER DEFAULT 0,
  state_bits INTEGER DEFAULT 0,
  mark_flag INTEGER DEFAULT 0
)
```

```sql
PRAGMA key='s-dbw1776853545473Goo';
SELECT state_bits, COUNT(*) AS cnt
FROM message
GROUP BY state_bits;
```

查询结果：

```text
state_bits=0  cnt=242
state_bits=1  cnt=5
```

推理：`conversation_member.is_deleted` 用于会话列表过滤；本题问“聊天记录/聊天数据删除”，应以 `message` 表的删除展示状态位为准。`state_bits=1` 的记录共 5 条，与 `_buildDeletedMessage` 删除展示逻辑对应。

结论：

| 问题 | 答案 |
| --- | --- |
| 删除聊天记录数量 | `5` |
| 显示聊天数据删除状态的字段 | `state_bits` |
